# Apollo AI Trader v2.8.0 — 依赖清单
# 安装: pip install -r requirements.txt

# ── 核心依赖 ──
futu-api>=8.0.0
vnpy>=3.9.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# ── 优化 ──
optuna>=3.4.0

# ── 调度 ──
apscheduler>=3.10.0

# ── Telegram ──
python-telegram-bot>=20.0

# ── Web 框架（管理面板用）──
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0

# ── 数据库 ──
# SQLite 内置于 Python，无需额外安装
# 如需 PostgreSQL:
# psycopg2-binary>=2.9.0

# ── 工具 ──
python-dotenv>=1.0.0
loguru>=0.7.0
redis>=5.0.0

# ── 测试 ──
pytest>=7.4.0
